'use strict'
/* global angular */

import ShopListController from './shared/shared.services'

/**
* For now, we will only use shared services, so we
* forward the inclusion directly.
* This might change later on
*/

export default ShopListController
