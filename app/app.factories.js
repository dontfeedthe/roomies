'use strict'
/* global angular */

import SharedFactories from './shared/shared.factories'

/**
* For now, we will only use shared services, so we
* forward the inclusion directly.
* This might change later on
*/

export default SharedFactories
